import 'package:flutter/material.dart';

class union extends StatelessWidget {
  const union({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Union Bank'),
      ),
    );
  }
}
